const list = document.querySelector('#book-list ul');

//delete books
list.addEventListener('click', function(e){
 // add books
 conast addForm = document.forms['add-book'];
 addForm.addEventListener('submit',function(e){
     
 })
    }
});

